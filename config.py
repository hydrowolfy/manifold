"""Default run parameters for experiments."""
DEFAULT_STEPS   = 800
DEFAULT_SEEDS   = (0, 1)
GROWTH_SEEDS    = (0, 1, 2)
DIM_FIT_RANGE   = (2, 6)        # r-range for the log-log ball-growth fit
DIM_MIN_NODES   = 60           # below this, dimension is not estimated
