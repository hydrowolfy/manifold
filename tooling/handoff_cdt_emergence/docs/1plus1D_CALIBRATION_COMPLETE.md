# 1+1D CDT — Calibration Complete

Both branches now have clean, controlled, honest results after the fix-round. No new universality class emerged — which is itself the finding. Independently spot-checked against the raw JSON/markdown.

## Branch A — off-manifold (baby-universe outgrowth), operator FIXED

The corrected move (a baby universe = a self-contained CDT cap grafted at a single neck vertex, adding volume behind a bottleneck with no shortcut) reverses the earlier pathology. Detailed balance is exact (flux imbalance 0.000e+00); D=0 reduces byte-identically to the validated substrate; the stale-index bug is gone.

Decisive test passed: as μ decreases and baby universes proliferate, **d_H rises monotonically 1.60 → 1.92 → 2.24 → 2.78** (reproduced across seeds, confirmed at N₂ = 1024/2048). Baby universes inflate the geometry toward the Euclidean d_H = 4 — the correct ALWZ direction, opposite to the old crumpling shortcut.

What the proxy does NOT deliver, stated honestly:
- **μ_c ≠ (3/2)ln N.** The grafts are an independent-site fugacity gas, so density follows a √(S·e^{−μ}) saddle and collapses under e^{−μ}·N₂ (exponent 1/2, slope a ≈ −0.7), not the ALWZ 3/2. Reproducing that exponent requires the faithful spatial-topology-change move (a slice S¹ splitting into two circles that share vertices and couple to the Pachner sector) — a full state rewrite, unbuilt.
- **d_s FALLS (1.15 → 0.73 → 0.57) while d_H rises** — the signature of a bottleneck/branched-polymer tree of caps, not the Liouville continuum (which is d_H=4, d_s=2). So the proxy confirms the direction but lands in a bottleneck phase, not on the ALWZ fixed point.

## Branch B — nonequilibrium (winding current), null CONFIRMED in the right sector

The current-sector check (structure-factor relaxation on the spatial density field, the observable the drive actually couples to) settles the earlier ambiguity:

- z_current^eq = 1.93 ± 0.02; **z_current(E=0.3) = 2.00 ± 0.09** — diffusive (Edwards–Wilkinson z=2), **5.5σ above KPZ 3/2**, identical to equilibrium.
- **Positive control z_current(E=2.0) = 1.71 ± 0.08**, bent toward KPZ, with a clean t^{2/3} current-variance window — proving the observable responds to a relevant drive. So the E=0.3 null is real, not a blind observable.
- Interpretation is textbook WASEP: weak asymmetry has a crossover length ℓ*(0.3) ≈ 340 ≫ accessible L=48, so E=0.3 sits on the diffusive side; KPZ is presumably the asymptotic class for any E>0 but is out of reach at linear-response drive.

Net: the drive is genuinely non-integrable (Kolmogorov confirmed) but irrelevant to the CDT geometry at accessible weak drive; strong drive is genuine KPZ with the geometry a spectator.

## What 1+1D has delivered (calibration, per the Skeptic's framing)

- Substrate validated: d_H → 2 (Ambjørn–Loll).
- Drive machinery validated: Kolmogorov non-integrability + WASEP crossover with a positive KPZ control.
- Off-manifold operator validated in direction: baby universes inflate geometry toward d_H = 4.

Both branches landed on known physics; neither produced a novel universality class. The two remaining 1+1D builds — A's faithful topology-change move (to nail the analytically-known (3/2)ln N and the Liouville certificate) and B's L≳340 rings (to reach the known weak-drive KPZ crossover) — chase answers that are already known analytically. Per the Skeptic's ruling, 1+1D is calibration, not discovery, and it is now calibrated.

## The decision

The genuinely novel questions — do off-manifold proliferation and a non-integrable drive do anything *unexpected* where there is no analytic anchor to reproduce — live in **2+1D CDT**, which is also the actual 3D-geometry goal. Options: (1) build 2+1D CDT and carry both validated branches into it; (2) first complete A's faithful topology-change move in 1+1D to bank the (3/2)ln N exponent; (3) both in parallel.
